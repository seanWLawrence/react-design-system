import { FC, ReactNode, PropsWithRef } from 'react';
interface BlockquoteProps {
    children: ReactNode;
}
export declare let Blockquote: FC<BlockquoteProps & PropsWithRef<JSX.IntrinsicElements['blockquote']>>;
export default Blockquote;
